{
  "recipe_name": "Thrust - Box",
  "description": "Pick up the box, level it, then thrust it forward",
  "steps": [
    { "step_id": 1, "action": "home", "description": "Reset arm to home position" },
    { "step_id": 2, "action": "pickup", "parameters": { "target": "box" }, "description": "Pick up the box" },
    { "step_id": 3, "action": "thrust", "parameters": { "target": "box" }, "description": "Level and thrust the box forward" }
  ]
}
